query AppCatalogs($after: String) {
    catalogs(first: 100, after: $after, query: "STATUS:ACTIVE") {
        nodes {
            id
            title
            status
        }
        pageInfo {
            hasNextPage
            endCursor
        }
    }
}
