mutation UpsertLocations($input: [LocationInput!]!) {
    upsertLocations(input: $input) {
        locations {
            id
            platformProvidedId
        }
        userErrors {
            code
            message
        }
    }
}
