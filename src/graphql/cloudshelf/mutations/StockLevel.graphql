mutation UpsertStockLevels($input: [StockLevelInput!]!) {
    upsertStockLevels(input: $input) {
        stockLevels {
            id
        }
        userErrors {
            code
            message
        }
    }
}
